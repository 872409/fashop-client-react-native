import React, { Component } from 'react';
import {
    StyleSheet,
    View,
    ScrollView,
    Text,
    Image
} from 'react-native';

export default class FixedBottom extends Component {
    static propTypes = {

    };
    static defaultProps = {

    };
    render() {
        return <View/>
    }
}
